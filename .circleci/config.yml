version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/openjdk:8-jdk
      - image: circleci/node:7

 
    steps:
      - checkout
      - run:
          name: The First Step
          command: echo 'hi'

